name: project_upgrader
description: Dart executable to upgrade very_good projects
version: 0.0.1
publish_to: none

environment:
  sdk: ^3.11.3

dependencies:
  args: ^2.7.0
  cli_completion: ^0.5.1
  mason_logger: ^0.3.3
  path: ^1.9.1
  pub_updater: ^0.5.0
  yaml_edit: ^2.2.4

dev_dependencies:
  build_runner: ^2.12.2
  build_verify: ^3.1.1
  build_version: ^2.1.3
  mocktail: ^1.0.4
  test: ^1.30.0
  very_good_analysis: ^10.2.0

executables:
  project_upgrader:
